# Migration verified with Python 3.10.20 in the existing rl_genrisk environment.
# Minimal third-party imports for the project scripts.
numpy==1.23.0
pandas==2.2.3
# GPU runner additionally uses torch; verified in the existing environment:
# torch==2.11.0+cu130 (CUDA build). Do not replace it with a CPU-only wheel.
